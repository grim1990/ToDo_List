@page
@model ToDo_List.Pages.ShoppingPages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Shopping[0].GroceriesId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Shopping[0].OtherProductsId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Shopping) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GroceriesId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OtherProductsId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
